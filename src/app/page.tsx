import { Widget } from "@/components/Widget";

export default function Home() {
  return (
    <main className="h-screen flex items-center justify-center bg-zinc-50 text-zinc-800 dark:bg-black dark:text-zinc-50">
      <Widget />
    </main>
  );
}
